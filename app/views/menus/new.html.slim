- provide :title, "#{Menu.model_name.human}#{t('new')}"

#form
  .text-center.mb-4
    h1 = "#{Menu.model_name.human}#{t('new')}"

  .card
    .card-body
      = render 'form', tenant: @menu

  = link_to t('back_to', action: t('index')), menus_path, class: 'btn btn-outline-secondary mt-2'